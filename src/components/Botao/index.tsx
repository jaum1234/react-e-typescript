import { Component } from "react";

import './style.scss';

class Botao extends Component
{
    render(): JSX.Element {
        return(
            <button className="botao">
                Botao
            </button>
        )
    }
}

export default Botao;